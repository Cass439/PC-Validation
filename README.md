# PC-Validation
This Streamlit app lets users upload a Partner Catalog spreadsheet, identifies inactive UPCs and updates the Price field to 0 for those rows, and returns a downloadable updated spreadsheet ready to upload back into the system.
